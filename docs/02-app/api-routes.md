# API Routes

See `docs/src/app/api.md` for the current API route map.

RB-050 adds project/image metadata APIs, RB-051 adds default slice/support APIs, and RB-052 adds minimal review/approval APIs. API hardening and export endpoints remain deferred.

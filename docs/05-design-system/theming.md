# Theming

RB-043 establishes CSS-variable theming through `src/design`.

Light and dark readiness should be token-driven. Components should use semantic classes such as `bg-background`, `text-foreground`, `border-border`, and shell/annotation token names.

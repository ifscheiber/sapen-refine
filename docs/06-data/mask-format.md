# Mask Format

Current mask serialization uses the `MSK1` header and `u8raw-v1` style label bytes through `src/mask/serialize.ts`.

RB-043 does not migrate mask format. Format normalization remains deferred.

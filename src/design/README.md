# SaPen Annotate Design Tokens

`tokens.css` defines the canonical CSS variables and Tailwind theme tokens.
`themes.css` owns global base styling.

Feature and shell components should consume semantic classes such as `bg-background`,
`text-foreground`, `border-border`, `bg-card`, and annotation token classes.
